# Produce the deliverable

Purpose: What-to-automate ladder, wrong-layer detection, one-client scoping guard.

## Role
Produce the deliverable for the `automation_triage` module.

## Inputs
Normalised inputs + the automation_triage API (detect_wrong_layer, health_check, initialize, scope_for_one_client, shutdown, triage).

## Definition of good output
A automation_triage output produced via its facade methods; deterministic where possible.

## Scripts

