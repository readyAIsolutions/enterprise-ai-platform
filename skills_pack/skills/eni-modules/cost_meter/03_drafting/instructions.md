# Produce the deliverable

Purpose: Cost Meter — per-tenant cost metering + fractional-reasoning policy (B3 + C4).

## Role
Produce the deliverable for the `cost_meter` module.

## Inputs
Normalised inputs + the cost_meter API (add_usage, health_check, initialize, meter, policy, remaining).

## Definition of good output
A cost_meter output produced via its facade methods; deterministic where possible.

## Scripts

