# Produce the deliverable

Purpose: Human review/approval gates inside agent runs.

## Role
Produce the deliverable for the `human_in_the_loop` module.

## Inputs
Normalised inputs + the human_in_the_loop API (audit_log, health_check, initialize, pending_escalations, queue_stats, resolve_action).

## Definition of good output
A human_in_the_loop output produced via its facade methods; deterministic where possible.

## Scripts

