# Produce the deliverable

Purpose: ENI Response Ops Module — self-healing fleet supervisor + ICM routing hook.

## Role
Produce the deliverable for the `response_ops` module.

## Inputs
Normalised inputs + the response_ops API (ENI Response Ops Module — self-healing fleet supervisor + ICM routing hook.).

## Definition of good output
A response_ops output produced via its facade methods; deterministic where possible.

## Scripts

