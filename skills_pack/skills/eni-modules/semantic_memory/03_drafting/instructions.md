# Produce the deliverable

Purpose: ENI Semantic Memory OS Module.

## Role
Produce the deliverable for the `semantic_memory` module.

## Inputs
Normalised inputs + the semantic_memory API (consolidate, db_path, dim, group_by_time_bucket, health_check, initialize).

## Definition of good output
A semantic_memory output produced via its facade methods; deterministic where possible.

## Scripts

