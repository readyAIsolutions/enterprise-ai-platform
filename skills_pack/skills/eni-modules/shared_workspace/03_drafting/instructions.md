# Produce the deliverable

Purpose: Live multi-editor workspace: lock, merge-safe write, history, redact-before-share.

## Role
Produce the deliverable for the `shared_workspace` module.

## Inputs
Normalised inputs + the shared_workspace API (health_check, history, initialize, list, lock, query).

## Definition of good output
A shared_workspace output produced via its facade methods; deterministic where possible.

## Scripts

