# Produce the deliverable

Purpose: Audit generated code for correctness, security and quality signals.

## Role
Produce the deliverable for the `codegen_audit` module.

## Inputs
Normalised inputs + the codegen_audit API (analyze_dependencies, audit, evaluate_task, health_check, initialize, review).

## Definition of good output
A codegen_audit output produced via its facade methods; deterministic where possible.

## Scripts

