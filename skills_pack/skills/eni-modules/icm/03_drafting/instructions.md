# Produce the deliverable

Purpose: ICM (Interpretable Context Methodology) — skill/prompt-engineering layer.

## Role
Produce the deliverable for the `icm` module.

## Inputs
Normalised inputs + the icm API (health_check, initialize, new_project, route, shutdown, stage_context).

## Definition of good output
A icm output produced via its facade methods; deterministic where possible.

## Scripts

