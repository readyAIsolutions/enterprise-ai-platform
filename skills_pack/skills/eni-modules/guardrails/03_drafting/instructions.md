# Produce the deliverable

Purpose: ENI Guardrails OS Module.

## Role
Produce the deliverable for the `guardrails` module.

## Inputs
Normalised inputs + the guardrails API (facade, get_event_bus, get_registry, guardrails, health_check, initialize).

## Definition of good output
A guardrails output produced via its facade methods; deterministic where possible.

## Scripts

