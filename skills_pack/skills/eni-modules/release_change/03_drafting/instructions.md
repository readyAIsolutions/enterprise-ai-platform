# Produce the deliverable

Purpose: Release & Change Management OS Module

## Role
Produce the deliverable for the `release_change` module.

## Inputs
Normalised inputs + the release_change API (health_check, initialize, shutdown).

## Definition of good output
A release_change output produced via its facade methods; deterministic where possible.

## Scripts

