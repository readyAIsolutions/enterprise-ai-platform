# Produce the deliverable

Purpose: ENI Vuln Scanner OS Module — offline LLM vulnerability scanning (garak-style).

## Role
Produce the deliverable for the `vuln_scanner` module.

## Inputs
Normalised inputs + the vuln_scanner API (ENI Vuln Scanner OS Module — offline LLM vulnerability scanning (garak-style).).

## Definition of good output
A vuln_scanner output produced via its facade methods; deterministic where possible.

## Scripts

