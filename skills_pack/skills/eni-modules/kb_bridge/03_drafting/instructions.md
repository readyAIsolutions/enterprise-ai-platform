# Produce the deliverable

Purpose: ENI Knowledge Base OS Module

## Role
Produce the deliverable for the `kb_bridge` module.

## Inputs
Normalised inputs + the kb_bridge API (bridge, health_check, health_checker, initialize, set_event_bus, shutdown).

## Definition of good output
A kb_bridge output produced via its facade methods; deterministic where possible.

## Scripts

