# Produce the deliverable

Purpose: Probe/evaluate model reasoning attributes (psychometric-style evals).

## Role
Produce the deliverable for the `model_psychometrics` module.

## Inputs
Normalised inputs + the model_psychometrics API (health_check, initialize, registry, runner, set_event_bus, shutdown).

## Definition of good output
A model_psychometrics output produced via its facade methods; deterministic where possible.

## Scripts

