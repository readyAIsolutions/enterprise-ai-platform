# Produce the deliverable

Purpose: ENI Unified Work System Module

## Role
Produce the deliverable for the `unified_work_system` module.

## Inputs
Normalised inputs + the unified_work_system API (check_consistency, health_check, initialize, render, set_event_bus, shutdown).

## Definition of good output
A unified_work_system output produced via its facade methods; deterministic where possible.

## Scripts

