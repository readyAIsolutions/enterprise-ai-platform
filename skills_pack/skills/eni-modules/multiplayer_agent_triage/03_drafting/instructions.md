# Produce the deliverable

Purpose: multiplayer_agent_triage — a Platform Kernel module for triaging problems and

## Role
Produce the deliverable for the `multiplayer_agent_triage` module.

## Inputs
Normalised inputs + the multiplayer_agent_triage API (critical_items, engine, health_check, initialize, record_win, set_event_bus).

## Definition of good output
A multiplayer_agent_triage output produced via its facade methods; deterministic where possible.

## Scripts

