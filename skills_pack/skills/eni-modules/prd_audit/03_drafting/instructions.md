# Produce the deliverable

Purpose: prd_audit — PRD-gated build workflow: write the PRD, audit it with a second

## Role
Produce the deliverable for the `prd_audit` module.

## Inputs
Normalised inputs + the prd_audit API (audit, audit_markdown, gate, gate_document, gate_markdown, health_check).

## Definition of good output
A prd_audit output produced via its facade methods; deterministic where possible.

## Scripts

