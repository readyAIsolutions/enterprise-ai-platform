# Produce the deliverable

Purpose: ENI Model Router OS Module — enterprise model-routing / fallback gateway.

## Role
Produce the deliverable for the `model_router` module.

## Inputs
Normalised inputs + the model_router API (ENI Model Router OS Module — enterprise model-routing / fallback gateway.).

## Definition of good output
A model_router output produced via its facade methods; deterministic where possible.

## Scripts

