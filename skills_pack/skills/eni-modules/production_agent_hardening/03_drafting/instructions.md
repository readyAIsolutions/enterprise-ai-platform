# Produce the deliverable

Purpose: Demo->production hardening linter across 8 scored dimensions + systems loops.

## Role
Produce the deliverable for the `production_agent_hardening` module.

## Inputs
Normalised inputs + the production_agent_hardening API (harden, health_check, initialize, readiness, shutdown, systems).

## Definition of good output
A production_agent_hardening output produced via its facade methods; deterministic where possible.

## Scripts

