# Produce the deliverable

Purpose: Operational hardening checks for shipping an agent into production.

## Role
Produce the deliverable for the `production_hardening` module.

## Inputs
Normalised inputs + the production_hardening API (assess, assess_answers, breaker, categories, check_determinism, check_drift).

## Definition of good output
A production_hardening output produced via its facade methods; deterministic where possible.

## Scripts

