# Produce the deliverable

Purpose: ENI Enterprise — Claude Code Tools Module v2.0.0

## Role
Produce the deliverable for the `agent_tools` module.

## Inputs
Normalised inputs + the agent_tools API (health_check, initialize, registry, shutdown).

## Definition of good output
A agent_tools output produced via its facade methods; deterministic where possible.

## Scripts

