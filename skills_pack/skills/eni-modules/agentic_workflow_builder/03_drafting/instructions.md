# Produce the deliverable

Purpose: agentic_workflow_builder — compose agentic workflows (Claude Code + Cursor).

## Role
Produce the deliverable for the `agentic_workflow_builder` module.

## Inputs
Normalised inputs + the agentic_workflow_builder API (compose_workflow, cost, create_agent, health_check, initialize, make_statement).

## Definition of good output
A agentic_workflow_builder output produced via its facade methods; deterministic where possible.

## Scripts

