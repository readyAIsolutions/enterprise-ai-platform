# Produce the deliverable

Purpose: Artifact Pipeline — navigate & organize all creative works (no LLM).

## Role
Produce the deliverable for the `artifact_pipeline` module.

## Inputs
Normalised inputs + the artifact_pipeline API (add, advance, all_renders, browse, derive, group_by).

## Definition of good output
A artifact_pipeline output produced via its facade methods; deterministic where possible.

## Scripts

