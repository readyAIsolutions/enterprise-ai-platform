# Produce the deliverable

Purpose: ENI Multi-Gateway Remote Control & Automations Module

## Role
Produce the deliverable for the `gateway` module.

## Inputs
Normalised inputs + the gateway API (gateway, health_check, initialize, scheduler, set_event_bus, shutdown).

## Definition of good output
A gateway output produced via its facade methods; deterministic where possible.

## Scripts

