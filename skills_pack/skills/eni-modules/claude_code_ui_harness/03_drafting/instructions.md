# Produce the deliverable

Purpose: Claude Code UI Harness — Enterprise Module wrapper.

## Role
Produce the deliverable for the `claude_code_ui_harness` module.

## Inputs
Normalised inputs + the claude_code_ui_harness API (audit_prd, build_prd, health_check, initialize, observe, render_prd).

## Definition of good output
A claude_code_ui_harness output produced via its facade methods; deterministic where possible.

## Scripts

