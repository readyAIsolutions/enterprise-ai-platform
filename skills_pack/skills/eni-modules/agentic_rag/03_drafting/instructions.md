# Produce the deliverable

Purpose: Agent-driven retrieval-augmented generation workflow.

## Role
Produce the deliverable for the `agentic_rag` module.

## Inputs
Normalised inputs + the agentic_rag API (answer, decompose, graph_info, health_check, initialize, rerank).

## Definition of good output
A agentic_rag output produced via its facade methods; deterministic where possible.

## Scripts

