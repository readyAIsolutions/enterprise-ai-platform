# Produce the deliverable

Purpose: ENI Agent Graph Module — langgraph-style stateful agent graph orchestration.

## Role
Produce the deliverable for the `agent_graph` module.

## Inputs
Normalised inputs + the agent_graph API (add_edge, add_node, build_graph, build_state_graph, facade, get_checkpoint).

## Definition of good output
A agent_graph output produced via its facade methods; deterministic where possible.

## Scripts

