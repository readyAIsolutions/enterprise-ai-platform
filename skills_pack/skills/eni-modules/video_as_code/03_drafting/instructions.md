# Produce the deliverable

Purpose: Represent/edit long-form video (animations) as code/scripts.

## Role
Produce the deliverable for the `video_as_code` module.

## Inputs
Normalised inputs + the video_as_code API (event_bus, health_check, initialize, run_pipeline, runner, set_event_bus).

## Definition of good output
A video_as_code output produced via its facade methods; deterministic where possible.

## Scripts

