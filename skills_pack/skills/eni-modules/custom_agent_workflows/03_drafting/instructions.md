# Produce the deliverable

Purpose: custom_agent_workflows — build your OWN AI coding workflows as a module.

## Role
Produce the deliverable for the `custom_agent_workflows` module.

## Inputs
Normalised inputs + the custom_agent_workflows API (build_initial_md, execute, health_check, initialize, plan, primer).

## Definition of good output
A custom_agent_workflows output produced via its facade methods; deterministic where possible.

## Scripts

