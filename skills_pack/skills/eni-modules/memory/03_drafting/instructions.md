# Produce the deliverable

Purpose: ENI Agent Memory OS Module.

## Role
Produce the deliverable for the `memory` module.

## Inputs
Normalised inputs + the memory API (DEFAULT_DB, add, consolidate, database_path, delete, forget).

## Definition of good output
A memory output produced via its facade methods; deterministic where possible.

## Scripts

