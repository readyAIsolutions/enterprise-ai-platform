# Produce the deliverable

Purpose: Observability — real, consolidated fleet health + Prometheus export.

## Role
Produce the deliverable for the `observability` module.

## Inputs
Normalised inputs + the observability API (bind_registry, health_check, initialize, metrics, shutdown, snapshot).

## Definition of good output
A observability output produced via its facade methods; deterministic where possible.

## Scripts

