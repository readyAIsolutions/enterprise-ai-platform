# Produce the deliverable

Purpose: voice_agent_hub — voice-driven orchestration of coding agents in a group call.

## Role
Produce the deliverable for the `voice_agent_hub` module.

## Inputs
Normalised inputs + the voice_agent_hub API (can_access_local_data, dispatch, grant_local_data_access, health_check, initialize, interrupt).

## Definition of good output
A voice_agent_hub output produced via its facade methods; deterministic where possible.

## Scripts

