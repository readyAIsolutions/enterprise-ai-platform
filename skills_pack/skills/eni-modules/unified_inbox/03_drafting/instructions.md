# Produce the deliverable

Purpose: Enterprise Platform Unified Inbox module package.

## Role
Produce the deliverable for the `unified_inbox` module.

## Inputs
Normalised inputs + the unified_inbox API (health_check, inbox, initialize, set_event_bus, shutdown).

## Definition of good output
A unified_inbox output produced via its facade methods; deterministic where possible.

## Scripts

