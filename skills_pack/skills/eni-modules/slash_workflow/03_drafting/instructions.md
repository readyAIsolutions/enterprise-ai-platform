# Produce the deliverable

Purpose: slash_workflow — Plan->Implement->Validate AI-coding workflow with reusable markdown slash-commands, a plan-document schema, isolated-context sub-agents, a dete

## Role
Produce the deliverable for the `slash_workflow` module.

## Inputs
Normalised inputs + the slash_workflow API (health_check, initialize, render_command, run_workflow, shutdown, validate_plan).

## Definition of good output
A slash_workflow output produced via its facade methods; deterministic where possible.

## Scripts

