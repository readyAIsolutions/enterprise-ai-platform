# Hand off the finished result

Purpose: slash_workflow — Plan->Implement->Validate AI-coding workflow with reusable markdown slash-commands, a plan-document schema, isolated-context sub-agents, a dete

## Role
Hand off the finished result for the `slash_workflow` module.

## Inputs
Verified slash_workflow output.

## Definition of good output
A clean, auditable deliverable/commit referencing which input produced it.

## Scripts
Commit on upgrade/demiurge-enterprise-boost (main is protected).
