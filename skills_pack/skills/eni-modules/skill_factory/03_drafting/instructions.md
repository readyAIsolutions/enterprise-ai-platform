# Produce the deliverable

Purpose: ENI Skill Factory Module — Meta-Skill Generator / Registry / Self-Evolution.

## Role
Produce the deliverable for the `skill_factory` module.

## Inputs
Normalised inputs + the skill_factory API (factory, health_check, initialize, set_event_bus, shutdown).

## Definition of good output
A skill_factory output produced via its facade methods; deterministic where possible.

## Scripts

