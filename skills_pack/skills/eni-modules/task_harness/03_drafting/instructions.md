# Produce the deliverable

Purpose: ENI Task Harness OS Module

## Role
Produce the deliverable for the `task_harness` module.

## Inputs
Normalised inputs + the task_harness API (harness, health_check, initialize, set_event_bus, shutdown).

## Definition of good output
A task_harness output produced via its facade methods; deterministic where possible.

## Scripts

