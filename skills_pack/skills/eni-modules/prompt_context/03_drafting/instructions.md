# Produce the deliverable

Purpose: Prompt & Context Management OS Module

## Role
Produce the deliverable for the `prompt_context` module.

## Inputs
Normalised inputs + the prompt_context API (health_check, initialize, shutdown).

## Definition of good output
A prompt_context output produced via its facade methods; deterministic where possible.

## Scripts

