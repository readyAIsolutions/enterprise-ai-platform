# Produce the deliverable

Purpose: ENI Response Hardening module.

## Role
Produce the deliverable for the `response_hardening` module.

## Inputs
Normalised inputs + the response_hardening API (apply_repair, get_audit, health_check, initialize, shutdown).

## Definition of good output
A response_hardening output produced via its facade methods; deterministic where possible.

## Scripts

