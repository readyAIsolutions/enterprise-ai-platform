# Produce the deliverable

Purpose: ENI A2A Module -- Agent-to-Agent protocol (Google A2A style).

## Role
Produce the deliverable for the `a2a` module.

## Inputs
Normalised inputs + the a2a API (create_task, facade, get_task, handoff, health_check, initialize).

## Definition of good output
A a2a output produced via its facade methods; deterministic where possible.

## Scripts

