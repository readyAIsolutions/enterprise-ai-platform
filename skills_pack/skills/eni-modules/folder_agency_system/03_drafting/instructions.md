# Produce the deliverable

Purpose: folder_agency_system — a folder/org-structure system that organizes an AI

## Role
Produce the deliverable for the `folder_agency_system` module.

## Inputs
Normalised inputs + the folder_agency_system API (create_agent, create_atlas, create_capability, create_project, create_workbench, health_check).

## Definition of good output
A folder_agency_system output produced via its facade methods; deterministic where possible.

## Scripts

