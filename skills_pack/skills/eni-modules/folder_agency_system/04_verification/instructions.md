# Verify

Purpose: folder_agency_system — a folder/org-structure system that organizes an AI

## Role
Verify for the `folder_agency_system` module.

## Inputs
The folder_agency_system deliverable + its expectations.

## Definition of good output
All checks pass: the module's own test suite is green and the result meets the definition of good output.

## Scripts
python3 -m pytest modules/folder_agency_system/tests -q  (REAL suite; must pass)
