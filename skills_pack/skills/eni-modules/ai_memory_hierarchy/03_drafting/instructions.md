# Produce the deliverable

Purpose: Layered memory model for agents (working/short/long) from AI-memory transcripts.

## Role
Produce the deliverable for the `ai_memory_hierarchy` module.

## Inputs
Normalised inputs + the ai_memory_hierarchy API (demote, health, health_check, hierarchy, initialize, insert).

## Definition of good output
A ai_memory_hierarchy output produced via its facade methods; deterministic where possible.

## Scripts

