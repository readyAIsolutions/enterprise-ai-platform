# Pull relevant context (progressive disclosure)

Purpose: Task -> {read/skip/skills} routing table with token-budget guard.

## Role
Pull relevant context (progressive disclosure) for the `context_routing` module.

## Inputs
Only the stage-relevant reference context needed to run this module.

## Definition of good output
The minimum context to run context_routing — no full-system dump.

## Scripts

