# Produce the deliverable

Purpose: Task -> {read/skip/skills} routing table with token-budget guard.

## Role
Produce the deliverable for the `context_routing` module.

## Inputs
Normalised inputs + the context_routing API (budget, health_check, initialize, route, shutdown).

## Definition of good output
A context_routing output produced via its facade methods; deterministic where possible.

## Scripts

