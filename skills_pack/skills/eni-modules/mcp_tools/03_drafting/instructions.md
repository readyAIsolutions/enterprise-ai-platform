# Produce the deliverable

Purpose: ENI MCP Tools Module — FastMCP-style tool registry & MCP serving layer.

## Role
Produce the deliverable for the `mcp_tools` module.

## Inputs
Normalised inputs + the mcp_tools API (call_tool, call_tool_async, get_tool, handle_request, health_check, initialize).

## Definition of good output
A mcp_tools output produced via its facade methods; deterministic where possible.

## Scripts

